# Region-resolved ML analysis of C K-edge XANES in doped graphene

This repository provides the public reproducibility workflow for the manuscript on
region-resolved machine-learning analysis of DFT-simulated C K-edge XANES spectra of
B- and N-doped graphene.

The main executable file is:

```text
public_reproducibility_workflow.ipynb
```

The notebook documents and reproduces the workflow from processed XAS/XANES spectra
to the machine-learning analyses, including:

- construction of the common-grid dataset;
- data dictionary and feature definitions;
- named spectral-window feature construction;
- random-forest classification and regression;
- configuration-grouped validation;
- feature-importance analysis;
- comparisons with baseline machine-learning models;
- comparisons with standard XANES descriptors.

## Important note: where to start

The released repository already includes the processed data files. Therefore, to
reproduce the results, start running the notebook from **Section 5:
"Interpolate to a common energy grid"**.

Sections 1–4 document the preprocessing pipeline from raw `.dat` spectra to
`data.csv`. They are included for transparency and should only be executed if raw
simulated spectra are available locally.

In short:

```text
If using released processed data:
    start from Section 5

If regenerating data from raw spectra:
    run Sections 1–4 first, then continue from Section 5
```

## Repository layout

A typical repository layout is:

```text
.
├── README.md
├── public_reproducibility_workflow.ipynb
├── data.csv
├── data_interpolated.csv
├── raw_xas/                         # optional raw simulated .dat spectra
├── processed_xas/
│   ├── smoothed/                    # generated if Sections 1–2 are run
│   └── normalized/                  # generated if Sections 1–3 are run
├── dimensionality_reduction_265_290/
├── xanes_descriptor_baseline/
└── baseline_summary.csv
```

The most important input files for reproducing the public workflow are:

| File | Description |
|---|---|
| `public_reproducibility_workflow.ipynb` | Main executable workflow. |
| `data.csv` | Processed per-spectrum table with paired energy/intensity columns and metadata. |
| `data_interpolated.csv` | Common-energy-grid table used for machine-learning and dimensionality-reduction analyses. |

The folders `processed_xas/`, `dimensionality_reduction_265_290/`, and
`xanes_descriptor_baseline/` are generated by the notebook when the corresponding
sections are executed.

## Installation

The workflow was written in Python and uses standard scientific Python packages.

A minimal environment can be created with:

```bash
conda create -n xanes-ml python=3.11
conda activate xanes-ml
pip install numpy pandas scipy scikit-learn matplotlib jupyter openpyxl
```

Optional packages:

```bash
pip install xgboost umap-learn
```

`xgboost` is only required for the XGBoost baseline models.  
`umap-learn` is only required for UMAP dimensionality-reduction plots.

## Running the workflow

Open the notebook:

```bash
jupyter lab public_reproducibility_workflow.ipynb
```

Then start execution from:

```text
Section 5. Interpolate to a common energy grid
```

The workflow is organized as follows.

| Section | Purpose | Usually run? |
|---|---|---|
| 1 | Raw XAS file convention | Documentation only if processed data are provided |
| 2 | Gaussian smoothing | Only if raw spectra are provided |
| 3 | Area normalization | Only if raw spectra are provided |
| 4 | Generate `data.csv` | Only if raw spectra are provided |
| 5 | Interpolate spectra to a common energy grid | Yes |
| 6 | Generate data dictionary | Yes |
| 7 | Load `data_interpolated.csv` and construct named features | Yes |
| 8 | Extract standard XANES descriptors | Optional / for descriptor baseline |
| 9 | PCA, UMAP, and t-SNE input construction | Optional |
| 9b | Dimensionality-reduction plots | Optional |
| 10 | Define train/test and grouped-validation splits | Yes |
| 11 | Random-forest manuscript experiments | Yes |
| 12 | Baseline models: RF, XGBoost, Lasso, MLP | Optional / reviewer-response analysis |
| 14 | Standard XANES descriptor baseline | Optional / reviewer-response analysis |

The notebook contains Boolean flags for longer analyses. Set them according to
the results you want to reproduce:

```python
RUN_RF_EXPERIMENTS = True
RUN_BASELINE_EXPERIMENTS = True
RUN_XANES_DESCRIPTOR_BASELINE = True
```

The descriptor baseline runs many model fits and may take several minutes. Set
`RUN_XANES_DESCRIPTOR_BASELINE = False` if you only want to reproduce the main
random-forest results.

## Dataset structure

### `data.csv`

`data.csv` is a per-spectrum wide table. Each row corresponds to one spectrum.

The spectral columns are stored as paired energy/intensity values:

```text
x1, y1, x2, y2, ..., xN, yN
```

These are followed by metadata and target columns.

### `data_interpolated.csv`

`data_interpolated.csv` is the main machine-learning input table.

It has a transposed layout:

- rows with numeric index values are photon energies in eV;
- each spectrum is one column;
- metadata rows are appended after the energy rows;
- the common grid uses 0.1 eV spacing;
- machine-learning features are the interpolated intensity values only.

## Data dictionary

The notebook generates a data dictionary in Section 6. The main fields are:

| Field | Meaning |
|---|---|
| `spectrum_name` | Spectrum/sample identifier. |
| `file_name` or `file_name.1` | Original spectrum file name when available. |
| numeric energy rows | Interpolated XANES intensity at the corresponding photon energy. |
| `type` | Dopant-family label: 0 = pristine, 1 = B-doped graphene, 2 = N-doped graphene. |
| `rate` | Dopant concentration used for classification. |
| `B_rate` | Boron concentration label. |
| `N_rate` | Nitrogen concentration label. |
| `mbl` | Mean nearest-neighbor dopant–carbon bond length. |
| `bader` | Mean dopant Bader charge. |
| `configuration_id` | Parent atomic configuration ID used for grouped validation. |
| `dopant_family` | Pristine, B-doped graphene, or N-doped graphene. |
| `concentration` | Parsed dopant concentration used for stratified splitting. |

## Spectral windows

All feature construction uses named energy windows rather than hard-coded
positional column slicing.

| Window | Energy range |
|---|---:|
| `PI` | 265.0–273.5 eV |
| `SIGMA` | 273.5–283.5 eV |
| `POSTEDGE` | 283.5–290.0 eV |
| `FULL` | 265.0–290.0 eV |

Only interpolated intensity values inside these windows are used as spectral
features.

## Splitting protocols

The notebook defines two evaluation protocols.

### 1. Spectrum-level concentration-stratified split

This is an 80/20 train/test split stratified by dopant family and concentration.
It is used to evaluate site-level predictive performance while preserving the
dopant/concentration distribution in the training and test subsets.

### 2. Configuration-grouped validation

This is a 5-fold grouped validation using atomic configuration ID as the grouping
variable. Spectra generated from the same atomic configuration are assigned to the
same fold. This prevents spectra from the same parent configuration from appearing
simultaneously in training and validation subsets.

## Machine-learning tasks

The workflow covers the following tasks.

### Classification

B- and N-doping concentration classifications are performed independently.
Pristine graphene is included as the 0% concentration class.

Metrics:

- accuracy;
- F1 score;
- fold-to-fold standard deviation where applicable.

### Regression

Two local descriptors are predicted:

- mean nearest-neighbor dopant–carbon bond length (`mbl`);
- mean dopant Bader charge (`bader`).

Metrics:

- R²;
- RMSE;
- MAE.

## Feature-importance analysis

For random-forest models trained on the full spectral range, feature importance is
computed as a function of photon energy.

These results are used to identify which spectral regions are most informative for:

- dopant concentration classification;
- mean dopant–carbon bond-length regression;
- mean Bader-charge regression.

## Baseline-model comparison

The notebook also includes baseline models used for reviewer-response analysis:

- random forest;
- XGBoost;
- Lasso or L1-regularized linear model;
- multilayer perceptron.

These models use the same spectral windows and splitting protocols as the main
random-forest workflow.

## Standard XANES descriptor baseline

Section 14 compares the region-resolved intensity representation with standard
XANES descriptors.

Descriptor groups include:

| Key | Representation | Description |
|---|---|---|
| `A_peak` | Peak-only descriptors | Peak energy and peak height for π*, σ*, and post-edge windows. |
| `B_area` | Area-only descriptors | Integrated areas, area ratios, and height ratios. |
| `C_edge` | Edge/centroid descriptors | Centroid energies, edge-derivative descriptors, half-maximum energy, and post-edge slope. |
| `D_all` | All standard descriptors | Union of all standard descriptors. |
| `E_pi_int` | π* intensity features | Interpolated intensity values in the π* window. |
| `E_full_int` | Full-spectrum intensity features | Interpolated intensity values in the full 265–290 eV range. |

Expected outputs from Section 14 include:

```text
xanes_descriptor_baseline/xanes_descriptors.csv
xanes_descriptor_baseline/sample_splits.csv
xanes_descriptor_baseline/descriptor_regression_summary_stratified.csv
xanes_descriptor_baseline/descriptor_regression_summary_grouped.csv
xanes_descriptor_baseline/descriptor_classification_summary_stratified.csv
xanes_descriptor_baseline/descriptor_classification_summary_grouped.csv
xanes_descriptor_baseline/descriptor_comparison_tables.xlsx
xanes_descriptor_baseline/descriptor_vs_intensity_regression_bar.png
xanes_descriptor_baseline/descriptor_vs_intensity_classification_bar.png
xanes_descriptor_baseline/conclusions.txt
```

## Expected outputs

Depending on which sections are executed, the notebook can generate:

| Output | Description |
|---|---|
| `processed_xas/smoothed/*.dat` | Gaussian-smoothed spectra with sigma = 1 grid point. |
| `processed_xas/normalized/*.dat` | Area-normalized spectra using Simpson integration. |
| `data.csv` | Per-spectrum wide table with energy/intensity pairs and metadata. |
| `data_interpolated.csv` | Common-grid table used for ML and dimensionality reduction. |
| `data_dictionary.csv` | Data-column and metadata-row definitions. |
| `dimensionality_reduction_265_290/dimensionality_reduction_coordinates_265_290.csv` | PCA, UMAP, and t-SNE coordinates. |
| `baseline_summary.csv` | Summary of RF, XGBoost, Lasso, and MLP results. |
| `xanes_descriptor_baseline/` | Descriptor-baseline outputs and comparison tables. |

## Notes on reproducibility

- Random seeds are fixed in the notebook where applicable.
- Spectral features are selected by energy-window names, not by positional column
  indices.
- Grouped validation uses parsed `configuration_id` values so that spectra from the
  same configuration remain in the same fold.
- The released processed dataset is sufficient to reproduce the machine-learning
  analyses from Section 5 onward.
- Sections 1–4 are retained to document how the processed tables can be regenerated
  when raw spectra are available.

## Citation

If you use this dataset or workflow, please cite the associated manuscript.
